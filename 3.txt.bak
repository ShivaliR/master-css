through useRef hookk, we can give reference to any element.
Single page application is very fast; only that component is reflected in website which is changed; so obv saves memory and burden on data.
No load symbol
1. So, css stylesheet can override the css of bootstrap.

Google Fonts

